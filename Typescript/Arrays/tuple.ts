let person: [string, number, boolean] = ["John Doe", 30, true]; 
console.log(person[0]); // Output: John Doe 
console.log(person[1]); // Output: 30  
console.log(person[2]); // Output: true




/***
 * 
 * Tuple - A tuple is a data structure that can hold a fixed number of values of different types. It is similar to an array, b
 * ut the types of the elements in a tuple are known and can be different from each other. 
 * In TypeScript, you can define a tuple using square brackets [] and specify the types of the elements in the tuple.
 * Example:
 * let person: [string, number, boolean] = ["John Doe", 30, true];
 * 
 */